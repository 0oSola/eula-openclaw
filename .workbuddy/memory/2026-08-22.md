# 2026-08-22

## Koleda V14D 交接接续（作为接收 agent）

- 验证交接文档 `docs/handoff/2026-08-22-koleda-v14d-rendering-progress.md`（未跟踪，未提交，保留原样）。
- 只读核对 `C:\w\rk3-face-v14d`：分支 `codex/koleda-v14d-face-shadow`、HEAD `387239b9`、暂存区空、152 条历史脏改动（不清理）、`trace.db` size/mtime 与交接一致（只读）。
- WGSL dist 默认核对通过：三个 hair 函数均为 fixed 常量、`spec_energy_gain=1.0`、`da_enabled=0.0`；注意 dist 内残留 `da_key_center` 惰性常量（`da_enabled=0` 时不生效）。
- 聚焦测试：hair-space-wgsl-state 13/13 通过；dedicated-area 5/5 通过（需 `--experimental-loader ./tests/reze-engine-esm-loader.mjs` 解析 TS 裸导入，直接 `node` 会报 ERR_MODULE_NOT_FOUND）。
- 正式 Gate 复核：`dedicated-area-gate-result.json` overallVerdict=NO-GO。
- Blender 5.1.1 在 `D:\Blender\blender.exe` 可用。
- 重要发现：Blender 侧宽带来源归因已由 `experiments/koleda-v14d-face-shadow/hair-spec-source-gate` 完成（Principled 各向异性高光×0.62 + toon×0.38，PROTO_Key 主驱动），下一步收窄为跨渲染器逐像素分量对齐。
- 已起草 spec：`C:\w\rk3-face-v14d\workflow\concepts\koleda-v14d-cross-renderer-component-forensic.zh-CN.md`（DRAFT，接缝 S1–S4 + 4 个开放项待用户确认）。
- 阻塞：交接 12.4 的跨任务交付握手需要 Codex 任务通知工具，本环境无此能力，已向用户报告。

## 拆票与 T1 阻塞

- 用户确认 spec 四项开放项：S1–S4 接缝、线性 EXR 重渲、允许实验开关、票据新建目录。
- 票据落地 `C:/w/rk3-face-v14d/workflow/tickets/koleda-v14d-component-forensic/`（01 EXR 闭合 / 02 Web 隔离 / 03 配准归因），spec 状态更新为 CONFIRMED。
- **T1 BLOCKED**：权威 blend `Koleda_V14D_DiscreteFaceShadow_NarrowBlendHysteresis.blend`（SHA 3995f2f7…c249）全盘（C:/w 全深度、D/E/F/G、Codex 文档目录）未找到；历史路径 `C:/w/koleda-gf2` 已不存在。fail-closed，不用替代 blend。
- 已用脚本与测试习惯：node 直接跑引用 TS 源码的 .mjs 测试需 `--experimental-loader ./tests/reze-engine-esm-loader.mjs`。

## 权威 blend 找回与纹理修复（T1 解除阻塞）

- blend 找回：Temp 目录 autosave（`_23236_autosave.blend`）→ 固化到 `C:/w/rk3-face-v14d/experiments/koleda-v14d-face-shadow/assets/`（SHA 1139617c…3cd4）。
- 六灯/World/相机/曝光/姿势与权威记录完全一致；渲染差异根因 = 18 张纹理指向已删除的 `C:/w/koleda-gf2`。
- 5 张面阴影状态图为 blend 内 packed，无需恢复；hair_spc + 12 张服装 n/rmo 用 `MMD project\MMD\克莱妲原皮...\Textures\New Folder\*_06_11_2026.png` 变体内存交换恢复（`component-exr-gate/repath_and_render.py`）。
- 恢复后 vs 08-17 权威 baseline：头发 pixel-identical（mean 0.004）、脸/白上衣 ≤4；外套/肩带/披风仍有差异（mean ~15）——服装 n/rmo 变体与原件内容不等价，原件全盘+回收站均无副本。
- 坑：bpy 缺失图像 filepath+reload 不生效，需 `images.load` + 交换 `node.image`；packed 图像 hasData=False 但渲染正常。
- 结论已写回 ticket 01（READY，已知降级标注）。待用户批准以修复后 blend 执行 T1，或提供 koleda_assets 备份。
