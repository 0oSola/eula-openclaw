# 游戏截图参考渲染迁移规格

## 目标与状态

2026-09-08主会话接管状态：当前材质、六灯、本机OCIO、五mask与参数面板已实施并完成独立本机验证；原ec23交付曾缺失材质公式，不作为完成证书。冻结预览源更新为cd973980e08e09739d620cc948472638a4e1c7f2。参数持久化采用用户/模型隔离localStorage，不占用Reze场景文档。真实登录后端、长期物理、全量动作门禁与第三方资源发布仍在完成边界之外。详见本轮交付文档。

将用户已认可的独立预览迁入共享 MMD 舞台，新增 `v14d-game` 渲染类型。本票已收窄为第一纵向切片：类型贯通、共享运行时的准备态接入/释放和聚焦验证；不是游戏着色器、材质公式或 Blender 数值等价证书。

### 第一票状态（2026-09-08）

- 前端类型、选择项、记忆白名单、校准路由、API `Literal`、SQLite 集中允许值和 `CHECK` 已贯通。
- 兼容迁移重建旧 `CHECK` 时保留模型路径、场景文档、更新时间和未知旧行；未知或空管线归一为 `classic`。
- 共享 `MMDCompanionRuntime` 对 `v14d-game` 使用显式准备态外观适配器；PMX 仍由共享 loader 加载，现有 VMD、口型、物理、相机和动作生命周期不复制。
- 适配器只提供 `install/release/status`，加载完成或切回旧类型时释放；运行时状态明确显示：`接入准备：真实游戏外观未迁移`。
- 已允许保留截图默认值作为后续配置定义，但本票不宣称这些默认值已经应用。

本票未完成、也不伪装完成：真实游戏外观与材质公式、六灯映射、OCIO/LUT/GLSL、本机白名单资产路由、视觉对齐、完整微调 UI、完整参数持久化、完整截图验收。

## 用户行为与默认值

用户能够选择新类型、看到已认可外观、继续使用现有动作和相机、调整参数、保存和恢复默认，并切回旧类型。默认显示为本机 OCIO，曝光 -0.40，织纹对比 2，纵向细线 1，目标闭眼，微笑，虹膜参考光感 2，脸部阴影自动选择开启，手动遮罩初值 2，六灯开启。圆盘实验关闭。头部、模型和主光旋转初值 0。

纵向细线只增强缝线，不分成两种底色。自动遮罩由实际角度派生，不固定状态 2。微笑使用口角形变，不覆盖眼部笑形变。

## 实现决策与不变量

- 复用共享 MMD 模型、动作、口型、相机和释放生命周期；不嵌入预览页面、不复制播放循环。
- 绘制期表情覆盖必须异常安全恢复，不破坏动作及说话权重。提供跟随动作语义。
- 六灯位置和尺寸按模型实际缩放映射；保持原辐亮度，不把尺寸扩大后的固定功率误作相同照明。
- 角色显示变换与背景分开；切换模式完整释放新模式资源、恢复旧模式配置。
- 本机 OCIO 派生资源许可未确认，仅存 Git 忽略缓存，不进入发布包；通过白名单资源接口读取，拒绝路径穿越。资源缺失明确不可用，不静默替代显示算子。
- 模型专用材质映射不得应用到不支持模型。
- 类型通过前端、API 和数据库校验与保存；微调按用户、模型、新类型独立版本化保存，不借用 Reze 场景文档。

## 纵向实施顺序

1. 第一票（本票）：选择新类型后进入共享 MMD 舞台准备态；真实 PMX 可加载，现有动作入口保持可用，切回旧类型释放准备态。阻塞：无。
2. 后续票：真实游戏外观、材质公式、灯光和本机资源路由。阻塞：1；另需许可确认和模型专用映射。
3. 后续验收票：真实动作、口型、相机、模式往返、缺失资源和视觉对齐。阻塞：1、2。

## 验收证据

本票提供实际运行命令、原生退出码、变更列表和未覆盖项。当前验收只覆盖：选择/记忆、API/数据库允许值与迁移、准备态状态、共享 runtime 接口接线、真实模型加载尝试、基础动作入口保持和切回旧类型释放。默认截图参数未应用，真实游戏外观、视觉对齐、非法/缺失本机资产拒绝和完整动作/口型/相机验收留给后续票。API 存根证据不得表述为真实后端联调；不得沿用旧完整验收证书。

## 冻结执行输入

源预览：`e388e32eb7a61734b581436637beef2e6f8260c2`，只读目录 `E:/codexWorktree/a40b/MMD project`。目标实现基线：`a38ccbf404ebf2501c1fa5c96df0146421080f91`。独占工作树 `E:/codexWorktree/v14d-game-stage`，分支 `codex/v14d-game-stage`。选择性移植，不合并包含历史失败诊断的整条源分支。主目录用户改动和 3142 预览不得触碰。

单步预期 60 分钟、90 分钟强制收口；同失败首次加两次有证据单变量重试。禁止未声明超过 20 分钟长命令及完整回放。失败交付检查点，不放宽标准。
