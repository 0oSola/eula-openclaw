# Dev and Release Stack Startup / 开发与发布栈启动

## Usage

```powershell
# Windows mode (default) - runs API and web on Windows
.\start-dev.ps1

# WSL mode - runs API and web inside WSL via wsl.exe
.\start-dev.ps1 -RunEnv wsl

# WSL mode with custom ports
.\start-dev.ps1 -RunEnv wsl -ApiPort 8100 -WebPort 3100

# Stop the running stack (auto-detects which env was used)
.\start-dev.ps1 -Action stop

# Check status
.\start-dev.ps1 -Action status
```

Or via the CMD wrapper:

```cmd
start-dev.cmd -RunEnv wsl
```

## What `--RunEnv wsl` does differently

1. **Path conversion**: Converts `CODEX_HOME`, `CODEX_WORKSPACE_*`, and `CODEX_WORKTREE_ROOT` in `api/.env` from Windows paths (e.g. `C:\Users\KSG\.codex`) to WSL paths (e.g. `/mnt/c/Users/KSG/.codex`).
2. **Process launch**: Starts `python3 -m uvicorn` and `npm run dev` inside WSL via `wsl.exe bash -lc`, instead of Windows `python.exe` / `npm.cmd`.
3. **Path restore on switch back**: When you run with `-RunEnv win` after a WSL session, any WSL paths (`/mnt/...`) in `api/.env` are automatically converted back to Windows paths.

## Prerequisites for WSL mode

- Python 3 and the API dependencies (`uvicorn`, `fastapi`, etc.) must be installed inside WSL.
- Node.js / npm must be available inside WSL.
- `codex` binary must be on PATH inside WSL (or set `CODEX_BIN` to its absolute WSL path).

## WSL2 localhost forwarding

WSL2 forwards `127.0.0.1` to Windows automatically, so the API and web ports are reachable from Windows browsers and the desktop pet without extra configuration.

## Release stack / 发布栈

便携发布推荐使用仓库根目录唯一入口：

    .\start-mmd.ps1 -Action package
    .\start-mmd.ps1 -Action start
    .\start-mmd.ps1 -Action status
    .\start-mmd.ps1 -Action stop

release/ 会保留每个带版本/时间戳的包目录和同名 ZIP。进入解压后的包目录后，包内 start-mmd.ps1 只使用包内 manifest、产物和 runtime 状态，不依赖源码目录。便携包不是安装器；目标机仍需 Python、Node.js/npm、Windows 图形环境和 API Python 依赖。

发布栈使用独立默认端口 API `8200`、Web `3200`，启动时先构建 Web production 和 desktop-pet production；Pet 直接加载本地 `desktop-pet/dist`，不依赖 Vite renderer。

常用命令：

```powershell
.\start-release.ps1 -Action build
.\start-release.ps1 -Action start
.\start-release.ps1 -Action start -SkipBuild
.\start-release.ps1 -Action status
.\start-release.ps1 -Action stop
```

可用的 release 参数：

- `-ApiPort`、`-WebPort`：监听端口，默认分别为 `8200`、`3200`。
- `-ApiHost`、`-WebHost`：绑定地址，默认 `127.0.0.1`。
- `-ApiBaseUrl`：Web/Pet 使用的 API base URL；省略时按 API 端口生成，也可继承已有 `MMD_PET_API_BASE_URL`。
- `-ApiDataDir`：API SQLite/日志数据目录；显式传入后严格使用该路径。未传入参数但存在 `API_DATA_DIR` 时严格使用环境变量路径；两者都未指定时检查 `api/data/sqlite/trace.db`，若缺失、是 Git LFS pointer 或不是有效 SQLite，则创建并回退到 `.runtime/release-stack/data`。
- `-PetReadyTimeoutSeconds`：等待 Electron renderer shell ready marker 的秒数；窗口不可自动观察时仍会报告本地文件证据和未验证边界。

运行时状态和日志：

- `.runtime/release-stack.json`：当前批次的端口、PID、命令身份、产物和健康状态。
- `.runtime/release-stack/<批次>/`：API、Web、Pet 日志及 `pet-ready.json`。
- `desktop-pet/dist/index.html`、`menu.html`、`notification.html`：Electron production runtime 的本地入口。

默认回退只适用于未指定 `-ApiDataDir` 且未设置非空 `API_DATA_DIR` 的 release build/start；显式数据目录出现无效 SQLite 时会直接失败，不会静默改写。

停止只验证并终止状态文件中记录、命令身份匹配的根进程及受控 runtime 子进程；不会按端口宽泛杀进程，也不会删除构建产物、API 数据或批次日志。
